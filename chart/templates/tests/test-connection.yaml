apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "acronyms.fullname" . }}-test-connection"
  labels:
    {{- include "acronyms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "acronyms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
