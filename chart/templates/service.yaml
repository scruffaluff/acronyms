apiVersion: v1
kind: Service
metadata:
  name: {{ include "acronyms.fullname" . }}
  labels:
    {{- include "acronyms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acronyms.selectorLabels" . | nindent 4 }}
